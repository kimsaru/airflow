원격 브랜치 목록 확인
git branch -r

로컬 + 원격 모두 보고 싶다면
git branch -a

원격 브랜치 삭제
git push origin --delete 브랜치이름

원격 브랜치 삭제 후 로컬 정리
git fetch -p

로컬 브랜치 삭제
git branch -d 브랜치이름
“merge 안 됐다는 걸 알고 있고, 다시 쓸 일도 없다”는 확신이 있을 때
git branch -d 브랜치명   # 안전 삭제 (merge 안 됐으면 거부)
git branch -D 브랜치명   # 강제 삭제 (상태 무시하고 삭제)

